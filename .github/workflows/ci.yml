name: CI Pipeline

on:
  pull_request:
    branches:
      - develop
      - main
  push:
    branches:
      - develop
      - main

jobs:
  ci:
    name: Lint, Format & Test
    runs-on: ubuntu-latest

    steps:
      # ── 1. Código ────────────────────────────────────────────────────────────
      - name: Checkout repository
        uses: actions/checkout@v4

      # ── 2. Entorno ───────────────────────────────────────────────────────────
      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          version: "latest"

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Cache uv dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/uv
          key: uv-${{ runner.os }}-${{ hashFiles('uv.lock') }}
          restore-keys: |
            uv-${{ runner.os }}-

      - name: Install dependencies (frozen lockfile)
        run: uv sync --frozen --all-extras

      # ── 3. Formato ───────────────────────────────────────────────────────────
      # Informativo: reporta archivos sin formatear sin bloquear el pipeline.
      - name: Ruff format check
        run: uv run ruff format --check . || true

      # ── 4. Lint ──────────────────────────────────────────────────────────────
      # Informativo: reporta issues sin bloquear el pipeline.
      - name: Ruff lint
        run: uv run ruff check . || true

      # ── 5. Score de calidad (informativo) ────────────────────────────────────
      # No bloquea el pipeline pero reporta el número de issues en etl/.
      # Útil para seguir la evolución de la deuda técnica en PRs.
      - name: Ruff score
        run: |
          ERRORS=$(uv run ruff check etl/ --output-format=concise 2>/dev/null | grep -c "^etl/" || true)
          TOTAL=$(find etl/ -name "*.py" | xargs wc -l 2>/dev/null | tail -1 | awk '{print $1}')
          echo "──────────────────────────────────"
          echo "  Ruff issues : $ERRORS"
          echo "  Lines of code (etl/): $TOTAL"
          echo "──────────────────────────────────"

      # ── 6. Docstrings (informativo) ──────────────────────────────────────────
      # Reporta cobertura de docstrings sin bloquear.
      # Cambiar `|| true` a `` para hacerlo bloqueante cuando el proyecto madure.
      - name: Docstring coverage (non-blocking)
        run: uv run ruff check . --select D || true

      # ── 8. Resumen del job ───────────────────────────────────────────────────
      # Imprime un resumen claro al final, visible en el log de GitHub Actions.
      - name: CI summary
        if: always()
        run: |
          echo "══════════════════════════════════════"
          echo "  CI Pipeline — data-product-forecasting"
          echo "══════════════════════════════════════"
          echo "  Branch : ${{ github.ref_name }}"
          echo "  Commit : ${{ github.sha }}"
          echo "  Runner : ${{ runner.os }} / Python 3.11"
          echo "══════════════════════════════════════"
